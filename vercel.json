{
  "framework": "nextjs",
  "buildCommand": "pnpm turbo build --filter=@markov/web...",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"]
}
